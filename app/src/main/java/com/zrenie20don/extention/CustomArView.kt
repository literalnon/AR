package com.zrenie20don.extention

import android.content.Context
import com.wikitude.architect.ArchitectView
import com.zrenie20don.R

class CustomArView(context: Context) : ArchitectView(context) {

    init {
        //inflate(getContext(), R.layout.architect_cam, null)
    }
}